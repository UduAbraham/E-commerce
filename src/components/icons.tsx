import { FiSun, FiMoon } from "react-icons/fi";

export const SunFilledIcon = ({ size = 22 }: { size?: number }) => (
  <FiSun size={size} />
);

export const MoonFilledIcon = ({ size = 22 }: { size?: number }) => (
  <FiMoon size={size} />
);
